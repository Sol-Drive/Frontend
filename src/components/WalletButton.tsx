import { WalletMultiButton } from '@solana/wallet-adapter-react-ui';

export const WalletButton = () => {
  return (
    <WalletMultiButton className="!bg-primary hover:!bg-primary/90 !rounded-lg !transition-all !font-medium" />
  );
};
